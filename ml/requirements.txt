# ml/requirements.txt — VaaniSetu ML Service dependencies

# Web framework
fastapi>=0.111.0
uvicorn[standard]>=0.29.0

# ASR: faster-whisper (CTranslate2 backend, ~4x faster than HF transformers on CPU)
faster-whisper>=1.0.1

# Audio processing (faster-whisper needs ffmpeg installed separately on the OS)
soundfile>=0.12.1
librosa>=0.10.1

# TTS: Google Cloud Text-to-Speech + Neural Fallbacks
google-cloud-texttospeech>=2.16.3
edge-tts>=6.1.0
gTTS>=2.5.0

# System & Utilities
psutil>=5.9.0
python-multipart>=0.0.9   # required for FastAPI File/Form uploads
python-dotenv>=1.0.1
pydantic>=2.7.0

# Testing
pytest>=8.2.0
pytest-asyncio>=0.23.7
httpx>=0.27.0             # needed for AsyncClient in tests

